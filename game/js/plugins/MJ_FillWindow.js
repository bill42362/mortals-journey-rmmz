//=============================================================================
// MJ_FillWindow.js
//=============================================================================
/*:
 * @target MZ
 * @plugindesc 讓桌面瀏覽器也預設「拉伸填滿視窗」（保持比例，左右可能留黑邊）。
 * @author Mortal's Journey
 *
 * @help MJ_FillWindow.js
 *
 * RMMZ 預設只有桌面打包版（NW.js）與行動裝置會自動拉伸填滿視窗；
 * 一般桌面瀏覽器（含 GitHub Pages 試玩）則以原生解析度置中顯示。
 *
 * 本外掛覆寫 Graphics._defaultStretchMode，讓桌面瀏覽器也預設啟用拉伸，
 * 使線上訪客一進入即填滿瀏覽器視窗（畫面比例維持遊戲原生比例，
 * 與視窗比例不符時左右／上下會留黑邊，屬正常）。
 *
 * 玩家仍可用 F3 切換拉伸、F4 切換全螢幕。
 *
 * 本外掛無參數，啟用即生效。
 */

(() => {
    "use strict";

    // 一律啟用拉伸（原本就為 true 的 NW.js／行動裝置不受影響，桌面瀏覽器改為啟用）。
    Graphics._defaultStretchMode = function() {
        return true;
    };
})();
